// This is to fix some issues with the syntax of move.
console.log("move.js loaded");

// let code = document.querySelectorAll("code");
// code.forEach(c => {
//     let spans = c.querySelectorAll("span");
//     spans.forEach(span => {
//         if (
//             span.innerText.split(" ").includes("public") ||
//             span.innerText.split(" ").includes("fun") ||
//             span.innerText.split(" ").includes("entry")
//         ) {
//             if (!span.getAttribute("style")) {
//                 span.style.color = "#ff79c6";
//             }
//         }
//     })
// })
